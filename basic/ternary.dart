void main() {

  //Ternary Operator: condition ? value1 : value2
  double grade = 8.0;
  String message = grade >= 5 ? "Approved" : "Reproved";

  int number = grade == 5.0 && grade != 7 ? 10 : 2;

  print(message);
  print(number); 
  
}